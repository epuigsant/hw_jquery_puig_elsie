$( document ).ready( function() {
    var paragraphs = $( 'p' );
    paragraphs.css( { 'font-family': 'sans-serif', 'lineHeight': 1.5,
    'color': 'blue', 'width': '600px', 'padding-left': '15px' } );
} );
